<h1><%= @user.name %></h1>
<p>Age: <%= @user.age %></p>
<p> Hometown: <%= @user.hometown %></p>

<%= button_to 'Edit', edit_user_path(@user), notice: 'Profile was successfully updated.'%> |

<%= button_to 'Delete', user_path(@user), method: :delete, data: { confirm: "Are you sure?" } %>

<%= button_to 'View All Trips', user_trips_path(@user), method: :get%>